<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>blog</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" />
</head>
<style>
  .hero {
    background-color: black;
  }
</style>

<body>
  <header>
    <nav class="bg-gray-800 fixed top-0 left-0 right-0 shadow-md">
      <div class="mx-auto max-w-7xl px-2 sm:px-6 lg:px-8">
        <div class="relative flex h-16 items-center justify-between">
          <!-- Logo -->
          <div class="flex items-start">
            <img class="h-8 w-8" src="https://www.redditinc.com/assets/images/site/reddit-logo.png" alt="Reddit Logo">
          </div>

          <!-- Right side buttons -->
          <div class="flex items-center space-x-4">
            <a href="#"
              class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Home</a>
            <a href="#"
              class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Popular</a>
            <a href="#"
              class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">All</a>
            <a href="#"
              class="text-gray-300 hover:bg-gray-700 hover:text-white px-3 py-2 rounded-md text-sm font-medium">Top
              Communities</a>

            <!-- Profile button -->
            <button
              class="bg-gray-800 p-1 rounded-full text-gray-400 hover:text-white focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-gray-800 focus:ring-white">
              <span class="sr-only">View profile</span>
              <svg class="h-6 w-6" fill="currentColor" viewBox="0 0 20 20" aria-hidden="true">
                <path d="M10 10a3 3 0 100-6 3 3 0 000 6zm2 1h-4a4 4 0 00-4 4v1h12v-1a4 4 0 00-4-4z" />
              </svg>
            </button>

            <!-- Login button -->
            <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-center">
              <a href="/login" class="ml-2 px-3 py-1 rounded-md bg-green-600 text-white hover:bg-green-500">Login</a>
            </div>

            <!-- Sign Up button -->
            <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-center">
              <a href="/signup"
                class="ml-2 px-3 py-1 rounded-md bg-yellow-600 text-white hover:bg-yellow-500">SignUp</a>
            </div>

            <!-- Logout button -->
            <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-center">
              <a href="/logout" class="ml-2 px-3 py-1 rounded-md bg-red-600 text-white hover:bg-red-500">Logout</a>
            </div>
          </div>
        </div>
      </div>
    </nav>
  </header>


  <main class="bg-gray-900 text-white min-h-screen pt-16 flex flex-col">
    <div class="container mx-auto px-4 py-8">
      <!-- Add Blog Section -->
      <div class="flex justify-center mb-8">
        <a href="/addblog" class="px-4 py-2 rounded-md bg-orange-500 text-white hover:bg-orange-400">Add Blog</a>
      </div>
      <div class="blogs grid grid-cols-1 md:grid-cols-2 lg:grid-cols-2 gap-6">
        <% blogs.forEach(blog=> { %>
          <div class="single-blog bg-gray-800 rounded-md shadow-lg overflow-hidden">
            <img src="<%= blog.blogImage %>" alt="<%= blog.title %>" class="w-full h-80 object-cover object-center"
              loading="lazy">
            <div class="p-4">
              <h2 class="text-xl md:text-2xl font-bold text-white mb-2 truncate">
                <%= blog.title %>
              </h2>
              <p class="text-gray-300 text-base mb-4 line-clamp-3">
                <%= blog.description %>
              </p>
              <div class="flex justify-between items-center">
                <div class="flex gap-2">
                  <a href="/updateblog/<%= blog._id %>"
                    class="bg-orange-500 text-white rounded-md px-4 py-2 hover:bg-orange-400">Update Blog</a>
                  <form action="/deleteblog/<%= blog._id %>" method="POST" class="inline">
                    <button type="submit" class="bg-red-600 text-white rounded-md px-4 py-2 hover:bg-red-500">Delete
                      Blog</button>
                  </form>
                </div>
                <div class="text-gray-400 text-sm md:text-base">
                  <%= blog.createdAt.toDateString() %>
                </div>
              </div>
            </div>
          </div>
          <% }); %>
      </div>
    </div>
  </main>




  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>